# 4-digit PIN required to access the web terminal
HERMES_PIN=1234

# Command to run inside the PTY (defaults to `hermes`)
# HERMES_CMD=hermes
# HERMES_ARGS=

# Bind address and port
# HOST=0.0.0.0
# PORT=3000
